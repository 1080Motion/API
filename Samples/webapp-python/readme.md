1080 motion API client python

Uses 
- Python 3.x
- Requests module
  How to install the module:
    In your project terminal which has python installed type: "pip3 install requests" or "pip install requests"
  
